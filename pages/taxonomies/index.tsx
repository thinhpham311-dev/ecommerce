import * as React from 'react';

export interface TaxonomiesPageProps {
}

export default function TaxonomiesPage (props: TaxonomiesPageProps) {
  return (
    <div>
      Taxonomy Pages
    </div>
  );
}
